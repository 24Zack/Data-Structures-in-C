/**
 *  Write a C program that asks the user to enter the grades of 20 students and stores them in
an array. By traversing the array, your program should display the following:
a. The average of the grades.
b. The lowest grade.
c. The highest grade.
*/
#include <stdio.h>
#define SIZE 5

int main(void)
{
    //declarations
    int arr[SIZE],low,max;
    float average;
    
}